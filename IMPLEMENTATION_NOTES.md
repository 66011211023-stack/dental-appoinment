# DentiCare Functional Build

เวอร์ชันนี้เปลี่ยนหน้าหลักจากข้อมูลตัวอย่างให้ทำงานกับ `dental_db` จริง

## เปิดระบบ

```bat
cd /d C:\dental-appoinment
docker compose up -d --build
docker compose ps
```

- เว็บไซต์: http://localhost:8080
- phpMyAdmin: http://localhost:8081
- phpMyAdmin user: `dental_user`
- phpMyAdmin password: `dental_pass`

## ทดสอบตามบทบาท

บัญชีเริ่มต้นทุกบัญชีใช้รหัสผ่าน `1234`

1. `admin`: เพิ่มบัญชี ผู้ป่วย สิทธิ์ วัสดุ นัดหมาย และดูรายงาน
2. `staff`: เพิ่มผู้ป่วย ซักประวัติ อนุมัตินัด และรับ/เบิกวัสดุ
3. `dentist`: ดูตาราง บันทึกการรักษา และเบิกวัสดุ
4. `patient`: จองคิว ดูสถานะ ประวัติ การแจ้งเตือน และแก้โปรไฟล์

ผู้ป่วยใหม่สมัครได้จากลิงก์ “สมัครสมาชิกผู้ป่วย” หน้า Login ช่องชื่อและนามสกุลแยกกัน ไม่มีช่อง Username ผู้ป่วยใช้อีเมลเข้าสู่ระบบ ระบบจะสร้าง Username ภายใน รวมชื่อเต็ม ตรวจอีเมล/เลขบัตรซ้ำ สร้าง HN และเข้าสู่ระบบให้อัตโนมัติ

บัญชีใหม่ใช้ bcrypt สำหรับรหัสผ่าน ส่วนบัญชีตัวอย่างเดิมที่ใช้ SHA-256 จะถูกอัปเกรดเป็น bcrypt อัตโนมัติหลังเข้าสู่ระบบสำเร็จ

หน้า Login และ Register มีปุ่มรูปตาสำหรับแสดงหรือซ่อนรหัสผ่านขณะกรอก

ระบบลืมรหัสผ่านค้นหาบัญชีผู้ป่วยจากอีเมล จากนั้นให้ตั้งรหัสผ่านใหม่ภายใน 15 นาทีและบันทึก bcrypt ลงฐานข้อมูลจริง สิทธิ์รีเซ็ตใช้ได้ครั้งเดียว สำหรับระบบใช้งานจริงควรเพิ่ม OTP ทางอีเมล

## ระบบนัดหมาย

- เจ้าหน้าที่หรือทันตแพทย์เพิ่มช่วงเวลาว่างจากหน้า “ตารางเวลาทันตแพทย์” ระบบตรวจวันย้อนหลัง เวลาสิ้นสุด และตารางทับซ้อน
- ผู้ป่วยเลือกได้เฉพาะช่วงเวลาที่ยังว่าง เมื่อส่งคำขอระบบล็อกช่วงเวลาทันทีและกันการจองซ้ำ
- Admin/Staff อนุมัติ ปฏิเสธ หรือเปลี่ยนสถานะนัดหมายได้ ผู้ป่วยได้รับการแจ้งเตือนเมื่อสถานะเปลี่ยน
- ผู้ป่วยยกเลิกนัดที่ยังไม่ถึงวันและยังไม่เสร็จสิ้นได้ หากยกเลิกหรือถูกปฏิเสธ ระบบคืนช่วงเวลากลับเป็น “ว่าง”
- ทันตแพทย์ทำเครื่องหมาย “เสร็จสิ้น” ได้เฉพาะนัดหมายของตนเอง

## ติดตั้งบน InfinityFree

- ใช้ `database/init_infinityfree.sql` เมื่อติดตั้งใหม่บน InfinityFree ไฟล์นี้ล้างข้อมูลเดิมและสร้างตารางใหม่ด้วย `utf8mb4` เพื่อรองรับภาษาไทย
- `database/init.sql` กำหนด charset/collation ของทุกตารางเป็น `utf8mb4` แล้ว

## ไฟล์สำคัญที่เพิ่ม

- `app/Core/Csrf.php` ป้องกันการส่งฟอร์มปลอม
- `app/Core/Actions.php` ประมวลผลคำสั่งที่แก้ฐานข้อมูล

## ข้อจำกัด

- การลืมรหัสผ่านสำหรับงานสาธิตยังไม่ส่ง OTP ทางอีเมลจริง
- ระบบนี้เหมาะกับต้นแบบ/โครงงาน ก่อนใช้งานจริงควรเพิ่ม SMTP/OTP, HTTPS, การสำรองข้อมูล และการเก็บ Audit log
- ควรสำรองฐานข้อมูลก่อนใช้ `docker compose down -v` เพราะคำสั่งนี้ลบข้อมูลทั้งหมด
